import {describe,it,expect} from 'vitest';
import {blankCard,initialLibrary} from './model';
import {cardSide,dailyProgress,sharedCard} from './review';
describe('shared media and daily progress',()=>{
 it('keeps image on back and independently reads each side',()=>{const c=blankCard({id:'d',areaId:'a',name:'Geral',language:'en-US',deleted:false,archived:false});c.front.text='Hello';c.back.text='Olá';c.back.image=new Blob(['img'],{type:'image/png'});c.front.audio=new Blob(['audio'],{type:'audio/webm'});c.front.mode='recording';expect(cardSide(c,false).image).toBeUndefined();expect(cardSide(c,true).image).toBe(c.back.image);expect(cardSide(c,false).audio).toBe(c.front.audio);expect(cardSide(c,true).audio).toBeUndefined();expect(cardSide(c,true).mode).toBe('reference');expect(cardSide(c,true).audioText).toBe('Olá')});
 it('counts distinct cards and uses the configured day boundary',()=>{const d=initialLibrary();d.preferences.timezone='America/Sao_Paulo';d.reviews=[{id:'1',cardId:'a',known:true,at:'2026-09-13T02:59:00Z'},{id:'2',cardId:'b',known:true,at:'2026-09-13T03:00:00Z'},{id:'3',cardId:'b',known:false,at:'2026-09-13T04:00:00Z'}];expect(dailyProgress(d,new Date('2026-09-13T05:00:00Z'))).toEqual({cards:1,attempts:2,correct:1});expect(dailyProgress(d,new Date('2026-09-14T05:00:00Z')).cards).toBe(0)});
 it('preserves back audio without copying it to the front',()=>{const c=blankCard({id:'d',areaId:'a',name:'Geral',language:'en-US',deleted:false,archived:false});c.front.mode='none';c.back.mode='file';c.back.audio=new Blob(['sound'],{type:'audio/mp4'});expect(sharedCard(c).front.audio).toBeUndefined();expect(sharedCard(c).back.audio).toBe(c.back.audio)});
});
